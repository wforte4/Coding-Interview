import styles from './page.module.css';

// You have the api http://localhost:3000/api/users
// Create a page that creates a new user using the components that are available in the app/src/components folder

export default function Home() {
  return (
    <div className={styles.main} />
  );
}
